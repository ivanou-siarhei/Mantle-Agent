import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "@/components/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Mantle Tokenized Equities Intelligence Agent",
  description: "AI-powered research assistant for the Mantle tokenized equities ecosystem: xStocks, Fluxion pools, RFQ flow, opportunities and natural-language insights.",
  keywords: ["Mantle", "xStocks", "Fluxion", "tokenized equities", "AI", "DeFi", "research"],
  authors: [{ name: "Mantle Intel" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
  openGraph: {
    title: "Mantle Tokenized Equities Intelligence Agent",
    description: "AI-powered research assistant for the Mantle xStocks ecosystem",
    siteName: "Mantle Intel",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
